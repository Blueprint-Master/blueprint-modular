import React,{useState} from 'react';import {createRoot} from 'react-dom/client';
import {FlagObject} from '../../../packages/core/src/objects/FlagObject';import {FlagControls} from '../../../packages/core/src/objects/FlagControls';import {DEFAULT_FLAG_SETTINGS,type FlagStyle} from '../../../packages/core/src/objects/flags';
function Preview(){const [flag,setFlag]=useState(DEFAULT_FLAG_SETTINGS),[style,setStyle]=useState<FlagStyle>('photorealistic'),[playing,setPlaying]=useState(true),[size,setSize]=useState(480);return <main><section><FlagObject flag={flag} style={style} playing={playing} size={size}/><p><button onClick={()=>setPlaying(!playing)}>{playing?'Pause':'Animer'}</button></p></section><section><label>Style<select value={style} onChange={e=>setStyle(e.target.value as FlagStyle)}><option value="photorealistic">Tissu réaliste</option><option value="illustration">Dessin</option></select></label><label>Taille<select value={size} onChange={e=>setSize(Number(e.target.value))}>{[160,320,480].map(n=><option key={n}>{n}</option>)}</select></label><FlagControls value={flag} onChange={setFlag}/></section></main>;}createRoot(document.getElementById('root')!).render(<Preview/>);
