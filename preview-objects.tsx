import {ObjectsCatalogue} from './components/site/ObjectsCatalogue';
import React,{useState}from'react';import{createRoot}from'react-dom/client';import{PlanetObject}from'./packages/core/src/objects/PlanetObject';import{PLANET_IDS,MOON_IDS}from'./packages/core/src/objects/universe';
const q=new URLSearchParams(location.search);
function Review(){const[id,setId]=useState(q.get('id')||'earth'),[style,setStyle]=useState(q.get('style')||'photorealistic'),[play,setPlay]=useState(false);return <main style={{color:'white',fontFamily:'sans-serif'}}><nav><select aria-label="Planet" value={id} onChange={e=>setId(e.target.value)}>{[...PLANET_IDS,...MOON_IDS].map(p=><option key={p}>{p}</option>)}</select><select aria-label="Style" value={style} onChange={e=>setStyle(e.target.value)}><option>photorealistic</option><option>illustration</option></select><button onClick={()=>setPlay(!play)}>{play?'Pause':'Play'}</button><button onClick={()=>{document.querySelector('canvas')!.toBlob(blob=>{if(!blob)return;const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`${id}-${style}.png`;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000);});}}>Export PNG</button></nav><PlanetObject id={id as any} label={id} size={600} playing={play} style={style as any}/></main>}
createRoot(document.getElementById('root')!).render(q.has("catalogue")?<div style={{background:"white",padding:32}}><ObjectsCatalogue/></div>:<Review/>);
